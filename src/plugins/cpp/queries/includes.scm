; Top-level preprocessor includes (quoted and angle-bracket paths).
(translation_unit
  (preproc_include
    path: (system_lib_string) @include.path))

(translation_unit
  (preproc_include
    path: (string_literal) @include.path))
