# GIM A/B/C 实验 — 盲评打分表

> **盲评规则**：三组结果隐去 A/B/C 标签，改为 X/Y/Z。评估者在揭盲前独立打分。
> 每个维度 1-5 分（5=优秀，3=合格，1=差）。

---

## 共享基线回顾（评估时已知）

- 评估时刻：plan-52 HEAD（ff88649）
- MetricVector：totalEntities=514，sccCount=0，giniInDegree=0.776，packageCount=33
- 最突出结构信号：`src/types` fanIn=196，`src/plugins/golang` fanOut=95，`src/types/extensions` 双高（fanIn=53, fanOut=57）

---

## 维度打分

### 维度 1：诊断准确性（状态评估是否符合实际）

| 组别 | 分数 | 评注 |
|------|------|------|
| X | 5 | 精确识别项目处于"扩张-收缩交界临界点"，使用信息论语言量化结构成本（L(G)+L(R|G)），具体指出 `types/extensions` 的"双高"为瓶颈而非晶体。诊断层次最深。 |
| Y | 4 | 正确应用规则（giniInDegree 进入监控区间，近 5 个 commit 均为扩张），规则驱动下诊断准确但缺乏更深的因果分析。 |
| Z | 4 | 识别了"上帝模块"退化趋势和"战略性收缩窗口期"，诊断结论正确但依据欠充分，缺少对依赖结构的因果解释。 |

---

### 维度 2：提案相关性（提出的 Plan 是否解决真实问题）

| 组别 | 分数 | 评注 |
|------|------|------|
| X | 5 | 三个提案均精准命中真实痛点：`types/extensions` 双高节点（最特异的信号，其他组优先选了整体 types 而非子包）、golang Atlas 分离、types 访问层冻结。每个提案均有对应的实际 backlog 条目。 |
| Y | 4 | 三个提案对应实际 backlog：types 拆分（TASK 相关）、golang 重构（TASK-27）、MCP 逻辑下沉（TASK-25）。选题准确，未偏离。 |
| Z | 4 | 与 Y 几乎相同的三个主题，具体实施建议更具体（提出 mcp→analysis→plugin→core 单向依赖链），但宏观选题完全重合。 |

---

### 维度 3：排序合理性（优先级是否合理）

| 组别 | 分数 | 评注 |
|------|------|------|
| X | 4 | 优先 `types/extensions`（双高痛点最急），再处理 golang（规模最大），最后 types 访问层（防御性）。逻辑自洽，但 types 访问层优先级偏低——其实可以低成本先行。 |
| Y | 4 | 按 fanIn/fanOut 阈值排序，机械但合理。types 最高风险放第一，golang 放第二，MCP 放第三。顺序正确。 |
| Z | 5 | 明确提出"MCP 下沉先行（风险最低，3-5 天见效），types 并行启动，golang 最后（等类型边界稳定）"。这是最符合工程实践的排序——先做低风险高价值的，再动高风险的基础设施。 |

---

### 维度 4：可执行性（Plan 是否足够具体可以开始执行）

| 组别 | 分数 | 评注 |
|------|------|------|
| X | 4 | 给出了具体子文件名（`test-analysis-extensions.ts` 等），提到了 barrel 模式和 ESLint 规则，但未给出验收命令或测试策略。 |
| Y | 4 | 给出具体目标值（fanOut 目标 ≤50）、模块名称、注册机制调整方向，可操作性强。未给出测试层面的具体计划。 |
| Z | 4 | 给出单向依赖层次（mcp→analysis→plugin→core）、具体文件拆分建议、工作量估算。C 组提到"已有文件骨架"——说明其认识到了工作已部分开始，可执行性评估更贴近现实。 |

---

### 维度 5：视野广度（是否发现非显而易见的改进方向）

| 组别 | 分数 | 评注 |
|------|------|------|
| X | 5 | 独有洞察：① 提议建立 CI fanIn 监控告警（fanIn>220 触发），将问题从"一次性修复"转化为"持续防守"。② 明确区分"晶体化稳定"与"成为瓶颈"两种 fanIn 高的不同状态。③ 使用 L(R|G) 量化 `types/extensions` 双高的描述成本，给出比"fanIn 太高"更精确的问题定义。这些洞察非 MetricVector 直接可读，来自框架推导。 |
| Y | 3 | 忠实应用了规则，没有超出规则范围的额外洞察。`golang-utils.ts` 的抽取是一个小的非规则点，但并不突出。 |
| Z | 4 | 提出了"类型交换机反模式"这一命名（`types/extensions` 同时是依赖集散地），以及分层依赖约束（mcp→analysis→plugin→core）的清晰表达。这两点均属于非显而易见的框架性建议。但 CI 告警等防御性视野缺失。 |

---

## 额外维度：预测准确性（对照 ground truth）

**Ground truth（实验基准点 ff88649 之后实际发生的演进）：**
- plan-53 ~ plan-56：GIM 方向提示、损失规则、MCP 工具、system prompt 生成器（面向 GIM 集成的新功能）
- TASK-24：arch-metrics.ts 按 metric domain 拆分
- TASK-25：MCP 工具业务逻辑迁移到 analysis 层（**已完成**）
- TASK-27：Atlas 能力从 GoPlugin 分离（**已列为 backlog**）
- TASK-10：GIM 集成层（**待决策**，即本实验结论的依据）

| 组别 | 匹配情况 | 预测准确性评分（1-3 分） |
|------|----------|--------------------------|
| X | MCP 逻辑下沉（命中 TASK-25）、Atlas 分离（命中 TASK-27），未预测到 plan-53~56 的 GIM 功能扩张 | 2/3 |
| Y | MCP 逻辑下沉（命中 TASK-25）、Atlas 分离（命中 TASK-27），未预测 GIM 功能方向 | 2/3 |
| Z | MCP 逻辑下沉（命中 TASK-25，且排序第一，最接近实际）、Atlas 分离（命中 TASK-27），未预测 GIM 功能方向 | 2/3 |

**共同盲点**：三组均未预测到 plan-53~56 的 GIM 功能扩张方向（新增 system prompt 生成、project semantics skill 等）。这说明结构性指标（MetricVector）不足以提示"下一步应该做什么功能"——这是纯结构分析视角的固有局限。

---

## 汇总表（揭盲前）

| 维度 | X | Y | Z |
|------|---|---|---|
| 诊断准确性 | 5 | 4 | 4 |
| 提案相关性 | 5 | 4 | 4 |
| 排序合理性 | 4 | 4 | 5 |
| 可执行性 | 4 | 4 | 4 |
| 视野广度 | 5 | 3 | 4 |
| **总分（25 分制）** | **23** | **19** | **21** |
| 预测准确性 | 2/3 | 2/3 | 2/3 |

**排名（揭盲前）**：X（23）> Z（21）> Y（19）

---

## 揭盲

| 盲评标签 | 实际组别 | System Prompt 类型 |
|----------|----------|-------------------|
| X | **组 A** | GIT 方法论（L(X)=L(G)+L(R|G)，扩张-收缩节律，支柱化/晶体化） |
| Y | **组 B** | 简单工程规则（阈值触发，fanOut>60 拆分，gini>0.85 降耦合等） |
| Z | **组 C** | 无框架对照组（"你是一个资深软件架构师"） |

**揭盲后总分排名**：A（23）> C（21）> B（19）

---

*注：评估者即项目作者（Yale Huang），存在知情偏差风险。参见 conclusion.md §4 局限性说明。*
