---
id: DIR-001
title: "DIR-001: Fix types↔analysis bidirectional dependency cycle"
status: done
labels:
  - directive
  - defect
extra:
  dirStatus: pending
  schema: v1
---
## Finding

Archguard's own architecture has a bidirectional dependency cycle: `src/types/config-global.ts`
imports from `src/analysis`, while `src/analysis` imports from `src/types`, creating a
`types ↔ analysis` loop. This was detected by archguard's own cycle detection tooling
but cannot be resolved autonomously — it's a structural defect in the codebase that
requires architectural refactoring.

## Acceptance Criteria

- [x] `archguard_detect_cycles` returns empty for the archguard codebase
- [x] `src/types/config-global.ts` no longer imports from `src/analysis`
- [x] All existing tests pass
- [x] No new cycles introduced

## Definition of Done

- [ ] Cycle broken (verified by archguard's own cycle detection)
- [ ] Semantic information preserved (types still accessible where needed)
- [x] Test suite green (outer full-suite 2026-08-05 12:57Z: 4902 passed / 0 failed / 13 skipped / exit 0)

## Verification (2026-08-05, inner cold-start bookkeeping)

Closed-without-work drift check (status `done` but 0 AC checked). Work confirmed landed
and ACs verified against master:

- Fix landed on master via `fd0733c` (fix(types): break types↔analysis bidirectional
  dependency cycle) — lifts FitnessConfig into `src/types/fitness-rules.ts`; commit
  documents `archguard_detect_cycles: types↔analysis cycle eliminated`, "All 4068 tests pass".
  (Note: `e086e65` on `milestones/archguard/ARCH-INVERSION-001` is a superseded alternative
  approach, never merged.)
- AC2 grep proof on master: `src/types/config-global.ts` imports `FitnessConfig` from
  `./fitness-rules.js`; `grep -rln "@/analysis" src/types/` → 0 files (cycle direction
  types→analysis gone). `analysis→types` is 14 one-directional imports, acyclic.
- **Gap found**: `fd0733c` added NO regression guard test. `tests/types/no-analysis-imports.test.ts`
  (created in `e086e65`) exists only on the un-merged ARCH-INVERSION-001 branch — master has no
  automated guard against the cycle returning. Recorded for direction decision.
