---
id: DIR-002
title: "DIR-002: TestCoverageRenderer.nodeId() collision — silently drops entities from Mermaid diagrams"
status: done
labels:
  - directive
  - defect
extra:
  dirStatus: pending
  schema: v1
---
## Finding

`TestCoverageRenderer.nodeId()` generates Mermaid node IDs from entity labels. Entity
labels that differ only in non-alphanumeric characters or truncation produce identical
node IDs, causing Mermaid to silently drop entities from diagrams. This affects quay's
visual architecture output — entities can be missing from coverage and dependency diagrams
without any error signal.

## Acceptance Criteria

- [x] Entity labels differing only in non-alphanumeric characters produce distinct node IDs
- [x] Truncated labels that collide produce distinct node IDs
- [x] Existing diagram output verified — no previously-dropped entities now missing from new output
- [x] Test added: two entities with colliding labels → both appear in diagram output

## Definition of Done

- [x] nodeId() collision fixed
- [x] Collision detection test added (RED→GREEN)
- [x] Affected quay diagrams regenerated and verified complete

---

## Audit (2026-07-25): VERIFIED — NO REFUTATION

**AC3 verified**: Coverage heatmap regenerated (Node 22.23.1). 6+ real entity name collisions
resolved — e.g. `ValidationError` in both `src/cli/errors/` and `src/core/interfaces/` now
have distinct node IDs and both appear in output.

**DoD3 verified**: `.archguard/output/test/coverage-heatmap.md` generated, all entities present.

**Root cause fix**: tree-sitter 0.25.0 native module incompatible with Node 25 V8 API
(`ObjectWrap`). Resolved by switching to Node 22.23.1 LTS.

## Verification (2026-08-05, inner cold-start bookkeeping)

Closed-without-work drift check re-verified. AC boxes now checked against master evidence:

- Fix landed on master via `ea13e9f` (fix(mermaid): resolve nodeId() collision that silently
  dropped entities) — `nodeId()` accepts optional `entityId` and appends a sanitized suffix.
  Commit message carries its own AC lines marked [x].
- Code on master confirmed: `src/mermaid/test-coverage-renderer.ts:50` calls
  `this.nodeId(entry.label, entry.entityId)`.
- Tests on master: `npx vitest run tests/unit/mermaid/test-coverage-renderer.test.ts`
  → 13 passed (2026-08-05, includes 2 collision-detection tests), covering AC1/AC2/AC4.
- AC3 (existing output verified) backed by the `## Audit (2026-07-25)` block above.

## Verification (2026-08-05, TASK-77 real-environment check)

The three unchecked DoD rows were re-verified against current master (worktree
`task/TASK-77` @ 5096769). All three are CONFIRMED satisfied; the DoD boxes are
intentionally left unticked per TASK-77 discipline (DoD rows are the outer close-out's
job), the conclusions are recorded here:

- **nodeId() collision fixed** — CONFIRMED. `src/mermaid/test-coverage-renderer.ts:92`
  `nodeId(label, entityId?)` appends a sanitized `entityId` suffix when present; all four
  call sites (lines 50/60/70/81) pass `entry.entityId`. Labels differing only in
  non-alphanumeric characters or truncation now produce distinct node IDs.
- **Collision detection test added (RED→GREEN)** — CONFIRMED.
  `tests/unit/mermaid/test-coverage-renderer.test.ts:185-228` contains the two collision
  tests (non-alphanumeric distinctness; truncated-label distinctness).
  `npx vitest run tests/unit/mermaid/test-coverage-renderer.test.ts` → 13 passed (includes
  the 2 collision tests).
- **Affected quay diagrams regenerated and verified complete** — CONFIRMED.
  `.archguard/output/test/coverage-heatmap.md` (generated 2026-07-25 12:30, after the fix
  `ea13e9f` at 12:17) contains both previously-colliding entities with distinct node IDs:
  `ValidationError_src_cli_errors_index_ts_ValidationError` (line 34) and
  `ValidationError_src_core_interfaces_validation_ts_ValidationError` (line 58) — no
  silently dropped entities.
