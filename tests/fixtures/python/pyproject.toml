[tool.poetry]
name = "test-project"
version = "0.1.0"
description = "Test Python project"

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.31.0"
numpy = "^1.24.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0.0"
black = "^23.0.0"
mypy = "^1.0.0"
