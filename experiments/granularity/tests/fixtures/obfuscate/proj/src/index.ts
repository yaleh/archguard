// Barrel: public re-exports (rename must not preserve public names via aliases).
export { SvgRenderer } from './mermaid/renderer';
export { parseDiagram, PARSER_VERSION } from './parser/parse';
export type { Diagram, Theme } from './types/model';
