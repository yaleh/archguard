# archguard quay-native task board
providers:
  native:
    path: /home/yale/work/archguard/tasks
gates: {}
loop:
  board: native
  stop: until(.halt)
  policy: ready-first
  execution: dispatched
  audit: adversarial
