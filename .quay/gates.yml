# DIR-050: gates config migrated to .quay/config.yml (gates: section).
# This file is kept as an empty stub for back-compat; the unified reader
# in registry.ts prefers the gates: section in config.yml when present.
